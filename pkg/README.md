# pkg/

Domain / Contract Layer — 公共契约、领域模型、服务接口。

| 目录 | 说明 |
|---|---|
| `model/` | 领域模型类型（Workspace, UModel, Entity, Query） |
| `contract/` | 服务接口定义（GraphStore, WorkspaceManager 等） |
| `errors/` | 共享错误类型与错误码 |
